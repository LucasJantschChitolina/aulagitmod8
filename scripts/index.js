
class ClasseExemplo{
    metodo(parametro){
        let counter = 0;

        for(counter; counter < 10; counter++){
            console.log("Este contador automático está em: " + counter);
        }
    }
}

export default new ClasseExemplo();